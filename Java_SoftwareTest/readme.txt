2015.03.09创建

// JOptionPane.showMessageDialog(null, "hello world!");//异常处理

//绝对布局
labela.setBounds(0, 0, 30, 10);
		labelb.setBounds(0, 200, 30, 10);
		label1.setBounds(0, 30, 30, 10);
		label2.setBounds(60, 30, 30, 10);
		label3.setBounds(120, 30, 30, 10);











//创建文本域框
JTextArea jta1 = new JTextArea(7,10);
JTextArea jta2 = new JTextArea(3,3);
/创建消息框
//JOptionPane jop1 = new JOptionPane("请输入今天的日期:"+'\n');
//JOptionPane jop2 = new JOptionPane("明天的日期为:"+'\n');

/*
 *frame1.setSize(800, 600); // 设置组件的大小
 *frame1.setLocation(10, 10); // 设置组件的显示位置(左上角坐标)
 *//frame1.setBounds(50, 50, 1000, 500);
 */
/*
 * // frame1.setSize(800, 600); // 设置组件的大小
 * Dimension dim = new Dimension(500, 400);
 * frame1.setSize(dim);
 */

/*
 * // frame1.setLocation(10, 10); // 设置组件的显示位置(左上角坐标)
 * Point point = new Point(200, 100); 
 * frame1.setLocation(point);
 */

/*
 * //现在所有的内容都加入到了JPanel1之中 
 * panel1.add(new JLabel("标签-A"));
 * panel1.add(label1);
 * panel1.add(new JButton("按钮-X")); 
 * panel1.add(button1);
 * // 将面板加入到窗体之上 
 * frame1.add(panel1); 
 * // frame.pack() ;// 根据组件自动调整大小
 */
/*
 * //设置布局
 * 1最屌的布局---坐标布局(想放那就放那)
 * 2组合布局(以下布局的多个组合)
 * frame1.setLayout(new FlowLayout(FlowLayout.CENTER,3,3)) ;
 * FlowLayout fl1 = new FlowLayout();//流式布局
 * //边界布局(frame默认布局)上下左右中	
 * BorderLayout bl1 = new BorderLayout(必须指定参数,不指定则组件会全填充<后面组件会覆盖前一个组件>); 
 * CardLayout cl1 = CardLayout();//选项卡布局
 * GridLayout gl1 = GridLayout();//网格布局
 * GridBagLayout gbl1 = GridBagLayout();//网格包 布局
 */
